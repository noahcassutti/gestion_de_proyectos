import { EstadoProyectoPipe } from './estado-proyecto-pipe';

describe('EstadoProyectoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoProyectoPipe();
    expect(pipe).toBeTruthy();
  });
});
