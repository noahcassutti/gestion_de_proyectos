worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout  65;


   limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

    server {
        listen       80;
        server_name  localhost;
        return 301 https://$host$request_uri;

    }

	
    server {
        listen       443 ssl;
        server_name  localhost;

        # --- CERTIFICADOS SSL ---
        ssl_certificate      "C:/Users/USUARIO/Downloads/nginx-1.30.2/nginx-1.30.2/conf/ssl/localhost+1.pem";
        ssl_certificate_key  "C:/Users/USUARIO/Downloads/nginx-1.30.2/nginx-1.30.2/conf/ssl/localhost+1-key.pem";

        ssl_session_cache    shared:SSL:1m;
        ssl_session_timeout  5m;
        ssl_ciphers  HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers  on;

        location / {
            root "C:/Users/USUARIO/Desktop/tpi_daw_gestiondeproyecto/frontend/dist/frontend/browser";
            index index.csr.html index.html index.htm;
            
            try_files $uri $uri/ /index.csr.html; 
        }

        location /api/ {
            # Cambia el 3000 por 4000 si tu backend de Node/Express corre en el 4000
            proxy_pass http://localhost:3000; 
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }
}